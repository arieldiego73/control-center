<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.controlcenter.controlcenter.dao.ClientDao">

    <select id="getAllClient" resultType="com.controlcenter.controlcenter.model.Client">
        SELECT * 
          FROM tbl_clients_mst
         WHERE del_flag = 0;
    </select>

    <insert id="addDepartment" parameterType="com.controlcenter.controlcenter.model.Client">
        INSERT INTO tbl_department_mst(dept_name, reg_id, update_id)
             VALUES (#{dept_name}, #{reg_id}, #{update_id});
    </insert>

    <update id="editDepartmentInfo" parameterType="map">
        UPDATE tbl_department_mst
           SET dept_name=#{department.dept_name}, reg_id=#{department.reg_id}, update_id=#{department.update_id}
         WHERE dept_id = #{id}
    </update>

    <update id="logicalDeleteDepartment">
        UPDATE tbl_department_mst
           SET del_flag=1
         WHERE dept_id = #{id}
    </update>
    
</mapper>
