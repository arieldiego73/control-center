<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- Mapper for tbl DevPhase   -->
<mapper namespace="com.controlcenter.controlcenter.dao.DevPhaseDao">

<!-- Fetch Data -->
    <select id="getAllDevPhase" resultType="com.controlcenter.controlcenter.model.DevPhase">
        SELECT *
         FROM tbl_dev_phase_mst
        WHERE del_flag = 0;
    </select>

<!-- Add Data -->
    <insert id="addDevPhase" parameterType="com.controlcenter.controlcenter.model.DevPhase">
        INSERT INTO tbl_dev_phase_mst(dev_phase_name, dev_phase_sh_name, reg_id, update_id)
             VALUES (#{dev_phase_name}, #{dev_phase_sh_name}, #{reg_id}, #{update_id});
    </insert>
    
<!-- Update Data -->
    <update id="editDevPhaseInfo" parameterType="map">
        UPDATE tbl_dev_phase_mst
           SET dev_phase_name=#{devphase.dev_phase_name}, dev_phase_sh_name=#{devphase.dev_phase_sh_name}, reg_id=#{devphase.reg_id}, update_id=#{devphase.update_id}
         WHERE dev_phase_id = #{id}
    </update>

<!-- Logical Delete Data -->
    <update id="logicalDeleteDevPhase" parameterType="map">
        UPDATE tbl_dev_phase_mst
           SET del_flag=1
        WHERE dev_phase_id = #{id}
    </update>

</mapper>